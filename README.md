1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
2. Ans: getElementById used for specific id selector, its use can one time, getElementsByClassName use for class selector, getElementsByClassName with this can select multiple claas. querySelector can use like css selector like (#) or (.), querySelectorAll used for all same name all property.
3. How do you create and insert a new element into the DOM?
4. Ans: To create and insert a new element into the DOM using JavaScript, the standard approach involves two main steps: creating the element and then appending it to an existing parent element.
5.  What is Event Bubbling? And how does it work?
6.  Ans: To create and insert a new element into the DOM using JavaScript, the standard approach involves two main steps: creating the element and then appending it to an existing parent element.
7.  What is Event Delegation in JavaScript? Why is it useful?
8.  Ans: Event delegation is a design pattern in JavaScript where a single event listener is attached to a parent element to manage events for all its descendant elements, rather than attaching a separate listener to each child.
9.  What is the difference between preventDefault() and stopPropagation() methods?
10. Ans: event.preventDefault() stops the browser's default action for an event, while event.stopPropagation() stops the event from moving further up or down the DOM hierarchy (event propagation)
